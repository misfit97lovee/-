# AI 독서 이해 에이전트 — 기본형 프로젝트 명세서

> 프로젝트 목적: 중학생이 책을 읽다가 이해하기 어려운 문장, 장면, 표현, 그림을 만났을 때 사진·복사한 글·직접 입력한 내용을 바탕으로 AI와 대화하며 스스로 이해하도록 돕는 독서 에이전트를 만든다.
>
> 사용 LLM: **Gemini 2.5 Flash**
>
> 개발 환경: **Antigravity 기반 바이브코딩**

---

# 0. 이 문서를 읽는 AI에게

너는 이 프로젝트를 구현하는 시니어 AI 에이전트 개발자다.

이 문서를 프로젝트의 기준 명세서로 사용하고, 임의로 핵심 요구사항을 바꾸지 않는다.

구현 전에는 반드시 현재 프로젝트 폴더 구조와 기존 파일을 확인한다. 기존 코드가 있다면 최대한 유지하면서 필요한 부분만 수정한다.

한 번에 지나치게 많은 기능을 만들지 말고, 이 문서의 **기본형 범위**를 먼저 완성한다.

사용자에게 보이는 실행 기록에는 모델의 숨겨진 사고 과정이나 긴 내부 추론을 노출하지 않는다. 대신 **판단 요약 → 실행한 행동 → 결과**만 짧고 이해하기 쉽게 기록한다.

Gemini API 키는 브라우저 코드에 직접 노출하지 않는다. `.env`에 저장하고 서버를 통해 호출한다.

책의 긴 원문을 답변에 그대로 재출력하기보다, 사용자가 제공한 내용을 이해시키는 데 필요한 만큼만 짧게 인용하고 나머지는 요약·설명한다.

---

# 1. 문제 정의

## 문제 상황

책을 혼자 읽는 중학생은 다음과 같은 상황에서 독서를 이어가기 어렵다.

- 어떤 문장이나 표현이 무슨 뜻인지 이해하기 어렵다.
- 문장은 읽었지만 장면이나 인물의 행동 이유가 잘 연결되지 않는다.
- 모르는 부분이 생겨도 바로 질문할 사람이 없다.
- 인터넷 검색을 하면 설명이 너무 어렵거나 책의 현재 문맥과 맞지 않을 수 있다.
- 단순히 정답을 받으면 이해한 것 같지만 스스로 생각하는 습관은 생기지 않는다.
- 읽은 내용을 나중에 다시 정리하려고 하면 어디부터 적어야 할지 막막하다.

## 한 문장 문제 정의

**중학생 독자는 책을 읽다가 이해하기 어려운 문장·장면·표현을 만났을 때 자신의 수준에 맞게 질문하고 설명해 줄 상대가 없어 독서 흐름이 끊기고, 읽은 내용을 스스로 정리하는 데 어려움을 겪고 있다.**

## 해결 방향

AI가 바로 답을 주는 것이 아니라 다음 흐름으로 돕는다.

1. 사용자가 책의 글 또는 사진을 입력한다.
2. AI가 사용자가 어떤 부분을 어려워하는지 먼저 묻는다.
3. 사용자가 현재 어떻게 이해했는지 짧게 확인한다.
4. 사용자가 스스로 생각할 수 있도록 힌트나 질문을 제공한다.
5. 필요하면 간단 설명 또는 자세한 설명을 제공한다.
6. 대화에서 확인한 내용은 현재 독서 세션의 상태로 기억한다.
7. 사용자가 원하면 지금까지 읽은 내용을 요약하거나 독서노트로 정리한다.

---

# 2. 프로젝트 목표

## 핵심 목표

중학생 사용자가 책을 읽다가 막힌 부분을 사진·복사한 글·직접 입력으로 보여주면, Gemini 2.5 Flash가 대화를 통해 어려운 지점을 파악하고 스스로 생각하도록 유도한 뒤 눈높이에 맞게 설명한다.

## 성공 기준

다음이 모두 동작하면 기본형 완성으로 본다.

- 첫 실행 시 학년을 질문한다.
- 책 내용을 직접 입력할 수 있다.
- 복사한 책 내용을 붙여넣을 수 있다.
- 책 페이지 사진을 업로드할 수 있다.
- AI가 바로 장문의 해설을 하지 않고 먼저 어려운 부분을 질문한다.
- 사용자의 생각을 한 번 이상 확인한 뒤 설명으로 이어질 수 있다.
- 설명을 `간단히`와 `조금 더 자세히` 두 단계로 전환할 수 있다.
- 현재 책과 대화에서 나온 중요한 정보를 세션 상태로 기억한다.
- `읽은 내용 요약`을 만들 수 있다.
- `독서노트`를 만들 수 있다.
- 새로고침 후에도 저장된 독서 세션을 다시 이어갈 수 있다.

---

# 3. 주요 사용자 페르소나

## 기본 정보

- 대상: 중학생
- 학년: 중1~중3
- 독서 수준: 책을 혼자 읽을 수 있지만 어려운 문장이나 문맥에서 자주 막힐 수 있음
- 환경: 집, 학교, 도서관 등에서 혼자 책을 읽음
- 기기: 노트북 또는 스마트폰 브라우저

## 현재 상황 예시

`진접이`는 중학생이고 소설을 읽고 있다.

책을 읽다가 어떤 문장을 읽었지만 왜 등장인물이 그런 행동을 했는지 이해하지 못했다. 인터넷에 문장을 검색해 봤지만 책 전체 맥락과 상관없는 설명이 나오거나, 설명 자체가 너무 어려웠다.

진접이는 페이지 사진을 찍어 AI에게 보여주고, 단순히 정답만 받는 대신 자신이 이해한 내용을 먼저 말해 본 뒤 부족한 부분을 설명받고 싶어 한다.

## 불만 상황

- 검색 결과가 너무 많다.
- 책의 문맥에 맞는 설명인지 판단하기 어렵다.
- 어려운 설명을 다시 검색해야 한다.
- 질문할 때마다 처음부터 책 내용을 다시 설명해야 하는 것이 번거롭다.
- 읽은 내용을 나중에 정리하려면 다시 책을 훑어봐야 한다.

## 요구사항

- 사진만 올려도 내용을 이해했으면 좋겠다.
- 어려운 부분부터 먼저 물어봐 줬으면 좋겠다.
- 바로 정답을 알려주기보다 내가 생각하도록 도와줬으면 좋겠다.
- 설명은 짧게 볼 수도 있고 필요하면 더 자세히 보고 싶다.
- 앞에서 내가 말한 책 내용과 어려워한 부분을 기억했으면 좋겠다.
- 읽은 내용을 자동으로 요약하고 독서노트로 정리하고 싶다.

## 한 문장 페르소나

**책을 혼자 읽을 수는 있지만 어려운 문장과 맥락에서 자주 막히며, 정답보다 자신의 생각을 발전시키는 대화를 통해 이해하고 싶어 하는 중학생 독자.**

---

# 4. 기본형 기능 범위

## 4.1 온보딩

처음 시작할 때 다음 정보를 수집한다.

필수:

- 학년

선택:

- 현재 읽는 책 제목
- 책의 종류: 소설 / 비문학 / 고전 / 기타

첫 질문 예시:

> 몇 학년이야? 학년에 맞춰 설명 난이도를 맞춰줄게.

학년을 받은 뒤:

> 지금 읽는 책 제목도 알려줄래? 아직 정하지 않았다면 바로 내용부터 보여줘도 돼.

---

# 5. 입력 방식

세 가지 입력을 모두 지원한다.

## 5.1 직접 입력

사용자가 책 내용을 직접 타이핑한다.

## 5.2 복사/붙여넣기

사용자가 책의 일부 문장이나 문단을 붙여넣는다.

## 5.3 이미지 업로드

책 페이지 사진을 업로드한다.

Gemini의 멀티모달 입력을 사용해 이미지 속 글과 시각적 요소를 함께 분석한다.

이미지를 분석한 뒤에는 곧바로 전체 해설을 하지 않는다.

먼저 다음과 같이 질문한다.

> 이 페이지에서 특히 이해가 안 되는 문장이나 장면이 어디야?

사용자가 특정 부분을 말하지 못하면 AI가 후보를 좁혀준다.

예:

> 문장 뜻이 어려운 거야, 인물이 왜 그렇게 행동했는지가 어려운 거야, 아니면 장면 전체가 잘 안 잡히는 거야?

---

# 6. 핵심 대화 원칙

## 원칙 1. 먼저 어려운 부분을 찾는다

책 내용이 들어왔다고 해서 바로 해설하지 않는다.

우선 사용자가 막힌 지점을 파악한다.

## 원칙 2. 사용자의 현재 생각을 확인한다

가능하면 다음과 같은 질문을 한 번 한다.

> 너는 이 부분을 지금 어떻게 이해했어?

또는

> 이 인물이 왜 이렇게 행동했다고 생각해?

단, 사용자가 `그냥 설명해줘`처럼 명확하게 바로 설명을 요구하면 불필요하게 여러 번 질문하지 않는다.

## 원칙 3. 스스로 생각하게 한다

먼저 작은 힌트나 질문을 제공한다.

예:

> 바로 앞 장면에서 이 인물이 어떤 기분이었는지 떠올려볼래?

그 뒤 사용자의 답을 보고 부족한 부분을 보완한다.

## 원칙 4. 설명은 두 단계

모든 설명은 기본적으로 두 단계 중 하나를 선택할 수 있게 한다.

### 간단히

- 2~4문장
- 핵심 의미만
- 쉬운 말
- 불필요한 배경지식 최소화

### 조금 더 자세히

- 핵심 의미
- 문맥
- 인물/사건과의 연결
- 필요한 배경지식
- 짧은 예시

답변 아래에는 필요할 때만 다음 버튼을 제공한다.

- `더 간단히`
- `조금 더 자세히`

---

# 7. 에이전트 5요소

## 7.1 목표 Goal

사용자가 책에서 이해하기 어려운 부분을 스스로 생각하고 이해할 수 있도록 질문, 힌트, 쉬운 설명을 제공하며, 읽은 내용을 기억해 요약과 독서노트 작성까지 돕는다.

## 7.2 계획 Plan

기본 처리 순서:

1. 학년 확인
2. 입력 형태 확인: 텍스트 / 붙여넣기 / 이미지
3. 책 내용 파악
4. 사용자가 어려워하는 부분 질문
5. 사용자의 현재 이해 확인
6. 힌트 또는 생각 질문 제공
7. 필요 시 설명 제공
8. 간단 설명 / 자세한 설명 전환 지원
9. 현재 세션 상태 업데이트
10. 요청 시 읽은 내용 요약
11. 요청 시 독서노트 생성

중요:

모든 단계가 항상 필요한 것은 아니다.

이미 정보가 있다면 다시 묻지 않는다.

## 7.3 상태 State

에이전트가 기억할 상태 예시:

```ts
interface ReadingState {
  student: {
    grade: '중1' | '중2' | '중3' | null;
  };

  book: {
    title: string | null;
    category: string | null;
  };

  session: {
    currentTopic: string | null;
    currentExcerptSummary: string | null;
    difficultPoints: string[];
    understoodPoints: string[];
    recentMessages: ConversationMessage[];
  };

  readingHistory: {
    summaries: ReadingSummary[];
    notes: ReadingNote[];
  };
}
```

### 저장 원칙

사용자가 제공한 책 전체를 별도 지식베이스에 자동 저장하지 않는다.

저장 대상은 **사용자와 AI가 실제로 주고받은 내용과 그 대화에서 만들어진 요약/노트**다.

이미지 원본은 기본적으로 장기 보관하지 않는다.

이미지에서 현재 대화에 필요한 정보를 분석한 후, 필요한 요약만 세션 정보로 남긴다.

기본형에서는 브라우저 `localStorage` 또는 프로젝트에 적합한 로컬 저장 방식을 사용한다.

## 7.4 도구 Tools

기본형에서는 다음 도구만 구현한다.

### `askUser`

목적:

- 학년 묻기
- 어려운 부분 묻기
- 사용자의 생각 묻기
- 추가 설명 여부 묻기

권한:

- 자유 실행

### `analyzeBookInput`

목적:

- 텍스트 분석
- 붙여넣은 문단 분석
- 이미지 속 글/그림 분석
- 현재 장면의 핵심 문맥 추출

입력 예시:

```json
{
  "type": "text | image",
  "content": "...",
  "grade": "중2"
}
```

출력 예시:

```json
{
  "shortContext": "이 장면의 핵심 요약",
  "possibleDifficultPoints": [
    "표현 의미",
    "인물 행동 이유"
  ]
}
```

### `explainReadingPoint`

목적:

사용자가 어려워한 부분을 학년에 맞춰 설명한다.

모드:

```ts
type ExplanationLevel = 'simple' | 'detailed';
```

### `makeReadingSummary`

목적:

지금까지 대화에서 확인한 범위 안에서 읽은 내용을 요약한다.

중요:

대화에서 확인되지 않은 내용을 임의로 책 전체 내용처럼 추가하지 않는다.

### `makeReadingNote`

목적:

대화 내용을 바탕으로 독서노트를 만든다.

기본 형식:

```md
# 오늘의 독서노트

## 읽은 부분

## 핵심 내용

## 이해하기 어려웠던 부분

## 새롭게 이해한 내용

## 내가 한 생각

## 다음에 읽을 때 기억할 것
```

## 7.5 종료 End

다음 조건 중 하나가 충족되면 현재 작업 흐름을 종료한다.

- 사용자가 `이해했어`, `됐어`, `고마워` 등으로 현재 설명 종료 의사를 보임
- 사용자가 다른 책/다른 부분으로 이동함
- 읽은 내용 요약 생성 완료
- 독서노트 생성 완료
- 사용자가 `그만`, `종료`를 요청함

대화 세션 자체는 유지할 수 있다.

---

# 8. TAO 실행 기록

화면에는 사용자가 이해할 수 있는 수준의 실행 기록을 보여준다.

**중요: 모델의 실제 내부 사고 과정이나 비공개 추론 과정은 출력하지 않는다.**

TAO는 아래처럼 짧은 작업 기록으로 사용한다.

## T — 판단 요약

예:

> 사용자가 인물의 행동 이유를 어려워하는 것으로 파악함.

## A — Action

예:

> 사용자의 현재 생각을 확인하는 질문을 보냄.

## O — Observation

예:

> 사용자가 인물이 화가 나서 행동했다고 이해하고 있음.

다음 단계:

> 앞 장면의 단서를 연결해 설명하기로 함.

화면에는 최근 기록 5~10개 정도만 보여도 된다.

---

# 9. 화면 구조

독서 서비스에 맞게 3영역 구조를 권장한다.

```text
┌─────────────────────────────────────────────────────────────┐
│                       상단 헤더                             │
│ 책 제목 / 학년 / 현재 독서 세션                            │
├─────────────────┬──────────────────────────┬────────────────┤
│ 책 내용 영역    │ AI 독서 대화             │ 독서 기록      │
│                 │                          │                │
│ 텍스트 입력     │ 사용자 메시지            │ 오늘의 요약    │
│ 붙여넣기        │ AI 질문                  │ 독서노트       │
│ 이미지 업로드   │ AI 설명                  │ TAO 기록       │
│                 │                          │                │
└─────────────────┴──────────────────────────┴────────────────┘
```

모바일에서는 세 영역을 탭으로 바꾼다.

- `책 내용`
- `AI 대화`
- `독서 기록`

---

# 10. 주요 UI 컴포넌트

권장 컴포넌트:

```text
App
├── Header
├── ReadingWorkspace
│   ├── BookInputPanel
│   │   ├── TextInput
│   │   ├── PasteInput
│   │   └── ImageUploader
│   │
│   ├── ChatPanel
│   │   ├── MessageList
│   │   ├── ChatInput
│   │   └── ExplanationLevelButtons
│   │
│   └── ReadingRecordPanel
│       ├── SessionSummary
│       ├── ReadingNotes
│       └── TaoLog
│
└── GradeOnboardingModal
```

---

# 11. Gemini 2.5 Flash 역할 프롬프트

아래 지침을 Gemini 시스템 프롬프트의 기준으로 사용한다.

```md
# Role

너는 중학생을 위한 AI 독서 이해 도우미다.

너의 목표는 책 내용을 대신 읽어주거나 정답만 알려주는 것이 아니라,
학생이 책에서 어려워하는 부분을 찾아 스스로 이해하도록 돕는 것이다.

# Core Behavior

1. 사용자의 학년을 알고 있지 않다면 먼저 학년을 묻는다.
2. 사용자가 책 내용이나 사진을 보내면 바로 장문의 해설을 하지 않는다.
3. 먼저 어떤 문장, 표현, 장면이 어려운지 확인한다.
4. 가능하면 사용자가 현재 어떻게 이해했는지 한 번 질문한다.
5. 사용자가 생각할 수 있는 짧은 힌트를 먼저 제공한다.
6. 설명이 필요하면 중학생 눈높이의 쉬운 말로 설명한다.
7. 설명은 기본적으로 짧게 하고, 사용자가 원하면 더 자세히 설명한다.
8. 사용자가 이미 말한 정보는 불필요하게 다시 묻지 않는다.
9. 현재 대화에서 확인한 내용만 바탕으로 읽은 내용 요약과 독서노트를 만든다.
10. 확실하지 않은 정보는 사실처럼 만들어내지 않는다.

# Explanation Levels

## simple
- 2~4문장
- 가장 중요한 의미만
- 어려운 표현 최소화

## detailed
- 핵심 의미
- 앞뒤 문맥
- 인물이나 사건과의 연결
- 필요하면 쉬운 예시

# Socratic Style

가능하면 먼저 학생의 생각을 끌어낸다.

예:
- "너는 이 문장을 어떻게 이해했어?"
- "바로 앞에서 이 인물은 어떤 기분이었을까?"
- "이 행동이 앞 장면과 어떤 관계가 있을까?"

하지만 사용자가 명확하게 "그냥 설명해줘"라고 하면 질문을 억지로 반복하지 말고 설명한다.

# Memory

현재 대화에서 다음을 참고한다.
- 학년
- 책 제목
- 지금 읽는 부분
- 어려워한 내용
- 이미 이해한 내용
- 이전 대화의 핵심 요약

책 전체 내용을 알고 있다고 가정하지 않는다.

# Reading Summary

요약 요청을 받으면 현재 대화에서 확인된 범위만 요약한다.

# Reading Note

독서노트 요청을 받으면 다음 형식을 사용한다.

- 읽은 부분
- 핵심 내용
- 이해하기 어려웠던 부분
- 새롭게 이해한 내용
- 내가 한 생각
- 다음에 읽을 때 기억할 것
```

---

# 12. API 구조

Gemini API 키를 프론트엔드에 직접 넣지 않는다.

권장 구조:

```text
Frontend
   ↓
/api/chat
   ↓
Local Node/Express Server
   ↓
Gemini 2.5 Flash API
```

## `.env`

예:

```env
GEMINI_API_KEY=YOUR_KEY_HERE
GEMINI_MODEL=gemini-2.5-flash
PORT=3001
```

`.env`는 Git에 올리지 않는다.

`.gitignore`:

```text
.env
node_modules
```

---

# 13. 서버 API 예시

## POST `/api/chat`

요청:

```json
{
  "messages": [],
  "readingState": {},
  "image": null
}
```

응답:

```json
{
  "message": "AI 응답",
  "statePatch": {},
  "tao": {
    "thoughtSummary": "어려운 부분을 확인할 필요가 있음",
    "action": "askUser",
    "observation": "질문을 보냄"
  }
}
```

실제 숨겨진 모델 추론 전문은 서버 응답에 포함하지 않는다.

---

# 14. 상태 저장

기본형에서는 별도 회원가입이나 DB를 만들지 않는다.

브라우저 `localStorage`에 다음을 저장한다.

```text
reading-agent-profile
reading-agent-current-session
reading-agent-reading-notes
reading-agent-summaries
```

## 저장하는 것

- 학년
- 책 제목
- 대화 기록
- AI가 만든 현재 문맥 요약
- 어려워했던 포인트
- 이해한 포인트
- 독서노트
- 읽은 내용 요약

## 기본적으로 저장하지 않는 것

- 원본 책 전체 파일
- 업로드한 이미지 원본 장기 저장
- 사용하지 않은 책 내용

---

# 15. 읽은 내용 요약 기능

사용자가 다음과 같이 말하면 실행한다.

- `지금까지 읽은 거 요약해줘`
- `내용 정리해줘`
- `오늘 읽은 부분 요약`

출력 예시:

```md
# 지금까지 읽은 내용

주인공은 ○○ 상황에서 △△를 경험했다.
그 과정에서 □□와 갈등이 생겼고,
현재는 ○○ 문제를 해결하려는 장면까지 읽었다.
```

AI가 현재 대화에서 확인하지 않은 다음 장면을 추측해 넣지 않는다.

---

# 16. 독서노트 기능

사용자가 다음과 같이 말하면 실행한다.

- `독서노트 만들어줘`
- `오늘 읽은 거 기록해줘`
- `내 생각까지 정리해줘`

예시:

```md
# 오늘의 독서노트

## 읽은 부분
주인공이 친구와 다툰 장면을 읽었다.

## 핵심 내용
두 사람은 서로 같은 사건을 다르게 받아들이고 있었다.

## 이해하기 어려웠던 부분
주인공이 갑자기 대화를 피한 이유가 잘 이해되지 않았다.

## 새롭게 이해한 내용
주인공은 단순히 화가 난 것이 아니라 상처받은 감정을 숨기려고 했다는 것을 알게 되었다.

## 내가 한 생각
나도 비슷한 상황에서 바로 말하지 못한 적이 있어서 조금 이해됐다.

## 다음에 읽을 때 기억할 것
두 인물이 다시 만났을 때 태도가 어떻게 달라지는지 보기.
```

학생이 직접 말하지 않은 감상은 사실처럼 대신 만들어 넣지 않는다.

필요하면 빈칸 또는 질문 형태로 남긴다.

---

# 17. 기본형 안전 규칙

기본형에서도 다음 최소 규칙은 코드와 프롬프트에 반영한다.

1. Gemini API 키를 클라이언트에 노출하지 않는다.
2. 모델이 책에 없는 내용을 본 것처럼 말하지 않는다.
3. 이미지 분석이 불명확하면 추측하지 않고 다시 찍어 달라고 안내한다.
4. 긴 책 원문을 그대로 재출력하는 기능은 만들지 않는다.
5. 사용자가 제공하지 않은 책 전체를 알고 있다고 가정하지 않는다.
6. 실패한 API 응답을 가짜 성공 결과로 바꾸지 않는다.

정교한 콘텐츠 가드레일과 승인 체계는 이후 안전 설계 단계에서 확장한다.

---

# 18. 기본형 개발 순서

## Phase 0 — 프로젝트 골격

- 프론트엔드 프로젝트 확인/생성
- Node/Express 서버 추가
- `.env` 설정
- Gemini 2.5 Flash 연결
- 기본 UI 레이아웃

완료 조건:

간단한 메시지를 Gemini에 보내고 응답을 화면에 표시할 수 있음.

## Phase 1 — 독서 입력

- 직접 입력
- 붙여넣기
- 이미지 업로드
- Gemini 멀티모달 요청

완료 조건:

세 방식의 입력 모두 AI가 인식할 수 있음.

## Phase 2 — 독서 에이전트 대화

- 학년 온보딩
- 어려운 부분 질문
- 사용자의 생각 확인
- 힌트
- 간단 설명
- 자세한 설명

완료 조건:

책 내용을 넣었을 때 바로 해설하지 않고 대화 흐름에 따라 이해를 돕는다.

## Phase 3 — 상태 기억

- ReadingState 구현
- 대화 정보 업데이트
- `localStorage` 저장
- 새로고침 복원

완료 조건:

앞에서 말한 학년, 책 제목, 어려운 부분을 같은 세션에서 다시 묻지 않는다.

## Phase 4 — 독서 기록

- 읽은 내용 요약
- 독서노트
- 기록 패널
- TAO 작업 로그

완료 조건:

사용자가 대화한 범위에서 요약과 독서노트를 생성하고 저장할 수 있다.

---

# 19. 기본 테스트 시나리오

## 테스트 1 — 첫 실행

사용자:

> 안녕

기대:

AI가 학년을 묻는다.

---

## 테스트 2 — 텍스트 입력

사용자:

> 나는 중2야.
>
> "그는 대답 대신 창밖만 바라보았다."

기대:

AI가 바로 의미를 단정하지 않고 무엇이 어려운지 묻는다.

예:

> 이 문장에서 표현 자체가 어려운 거야, 아니면 왜 이 인물이 대답하지 않았는지가 궁금한 거야?

---

## 테스트 3 — 생각 유도

사용자:

> 왜 대답 안 했는지가 궁금해.

기대:

AI:

> 바로 앞에서 어떤 일이 있었는지 떠올려볼래? 이 인물이 대답하기 불편해질 만한 일이 있었어?

---

## 테스트 4 — 간단 설명

사용자:

> 그냥 설명해줘.

기대:

2~4문장으로 중2 수준에서 간단히 설명한다.

---

## 테스트 5 — 자세한 설명

사용자:

> 좀 더 자세히.

기대:

앞뒤 문맥과 감정, 행동의 연결을 더 자세히 설명한다.

---

## 테스트 6 — 이미지

사용자:

책 페이지 사진 업로드.

기대:

- 이미지 내용을 분석한다.
- 읽을 수 없는 부분이 있으면 솔직하게 말한다.
- 전체 해설 전에 어려운 지점을 질문한다.

---

## 테스트 7 — 기억

앞 대화에서 사용자가 `중2`, 책 제목 `날개`라고 말함.

이후 새 메시지:

> 아까 그 부분 다시 설명해줘.

기대:

학년과 책 제목을 다시 묻지 않는다.

---

## 테스트 8 — 읽은 내용 요약

사용자:

> 지금까지 읽은 내용 요약해줘.

기대:

현재 대화에서 실제로 확인한 내용만 요약한다.

---

## 테스트 9 — 독서노트

사용자:

> 독서노트 만들어줘.

기대:

대화에서 확인한 내용과 사용자의 생각을 바탕으로 노트를 생성한다.

사용자가 말하지 않은 감상은 임의로 작성하지 않는다.

---

# 20. Antigravity에 처음 줄 구현 프롬프트

아래 프롬프트를 Antigravity에 그대로 사용할 수 있다.

```md
현재 프로젝트 폴더의 `READING_AGENT_SPEC.md`를 먼저 처음부터 끝까지 읽어줘.

이 파일을 이번 프로젝트의 기준 명세서로 사용해.

우리가 만들 것은 Gemini 2.5 Flash 기반의 중학생용 AI 독서 이해 에이전트야.

핵심 흐름은:

책 내용 입력(직접 입력 / 붙여넣기 / 사진)
→ 어떤 부분이 어려운지 질문
→ 사용자의 현재 생각 확인
→ 스스로 생각할 수 있는 힌트
→ 간단 설명 또는 자세한 설명
→ 대화 상태 기억
→ 읽은 내용 요약 / 독서노트

중요:

- Gemini API 키는 프론트엔드에 노출하지 말 것
- `.env`에서 키를 읽고 로컬 Node/Express 서버가 Gemini를 호출할 것
- 사용자가 제공한 책 전체를 별도 지식베이스에 저장하지 말 것
- 현재 대화에서 주고받은 내용과 그 요약/노트만 상태로 저장할 것
- 업로드한 이미지 원본은 장기 저장하지 말 것
- 실행 기록은 모델의 숨겨진 사고 과정을 출력하지 말고 `판단 요약 / 행동 / 결과`만 보여줄 것
- 책 내용을 넣었다고 바로 장문의 설명을 하지 말고 먼저 어려운 부분을 질문할 것

먼저 코드를 수정하지 말고 다음을 해줘.

1. 현재 프로젝트 폴더 구조와 파일을 확인
2. 현재 구현 상태를 분석
3. READING_AGENT_SPEC.md 기준으로 필요한 작업을 Phase 0~4로 나눠 계획 작성
4. 새로 만들 파일과 수정할 파일 목록 작성
5. 각 단계의 완료 조건 작성

계획을 먼저 보여줘. 내가 확인하면 실제 구현을 시작해.
```

---

# 21. 이번 기본형에서 만들지 않는 것

초기 버전의 범위를 지키기 위해 다음 기능은 이후 단계로 미룬다.

- 외부 서점 API 기반 책 추천
- 책 전체 PDF 자동 RAG
- 전국 도서관 검색
- 사용자 계정/로그인
- 클라우드 DB
- 선생님용 관리자 페이지
- 성적 평가
- 자동 독후감 완성본 제출 기능
- 음성 대화
- 복잡한 학습 분석 대시보드

기본형이 안정적으로 동작한 뒤 추가한다.

---

# 22. 최종 기본형 사용자 흐름

```text
[처음 접속]
      ↓
"몇 학년이야?"
      ↓
[중2]
      ↓
책 제목 입력 또는 바로 책 내용 입력
      ↓
텍스트 / 붙여넣기 / 사진
      ↓
AI가 내용 파악
      ↓
"어디가 제일 어려워?"
      ↓
사용자가 어려운 부분 선택
      ↓
"너는 이 부분을 어떻게 이해했어?"
      ↓
사용자 생각
      ↓
짧은 힌트
      ↓
간단 설명
      ↓
[조금 더 자세히] 선택 가능
      ↓
상태 저장
      ↓
계속 독서
      ↓
"지금까지 요약해줘"
또는
"독서노트 만들어줘"
      ↓
읽은 내용 요약 / 독서노트 저장
```

---

# 23. 프로젝트 한 문장 정의

**책을 읽다가 막힌 중학생에게 정답부터 주지 않고, 대화를 통해 스스로 이해하게 만든 뒤 읽은 내용을 기억하고 정리해 주는 AI 독서 파트너.**
